# Saker Browser Video Editor — Environment Variables
#
# This is a static-export Next.js app mounted at /editor/ by the Go server.
# No NEXT_PUBLIC_ variables are needed for the default setup.
#
# basePath is set to "/editor" in next.config.ts — the Go server handles
# the routing; the editor does not need to know its own mount point.