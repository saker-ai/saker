# Example 14: Artifact Pipeline

This example shows a lightweight artifact-first pipeline run using the multimodal runtime primitives.

It demonstrates:

- pipeline step execution through `api.Request.Pipeline`
- generated artifact refs in `Result.Artifacts`
- structured payloads returned alongside final text output
