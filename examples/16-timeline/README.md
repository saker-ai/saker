# Example 16: Timeline

This example shows how to inspect the multimodal execution timeline returned by the runtime.

It demonstrates:

- timeline entries on pipeline-backed runs
- cache hit/miss visibility
- tool call/result and artifact events
