# Example 15: Resumable Review

This example shows a checkpointed review flow.

It demonstrates:

- pipeline checkpoints
- `Result.CheckpointID`
- resuming a pipeline with `Request.ResumeFromCheckpoint`
